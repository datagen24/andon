---
description: Load project context now — read the memory index + recent state before continuing.
---

Orient yourself before we go further:

1. Read `memory/MEMORY.md` (the index).
2. Read `CONTEXT.md` if it exists (Running / Done / Next / Blockers).
3. Read the 1–2 most recent entries under Recent.

Then give me a **3-line summary**: where things stand + the single highest-leverage next move. Don't start work yet — just orient.

*(If you installed the `auto_orient.py` hook, this happens automatically at session start — use this command to re-orient mid-session or after a context reset.)*
