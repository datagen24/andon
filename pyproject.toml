# Test configuration only — andon ships ZERO runtime dependencies (stdlib Python 3).
# This file exists so `pytest` discovers and runs the suite the same way everywhere.
[tool.pytest.ini_options]
testpaths = ["hooks/tests"]
python_files = ["test_*.py"]
